<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!--
         HOTFIX 2026-08-02: Revert extractNativeLibs back to true.
         On many devices, exec() of a lib that's mmapped-in-place from the APK
         fails because the kernel page-guard blocks PROT_EXEC on APK-backed pages,
         or the path is "noexec" mounted. Keeping this true means the system
         extracts all .so to /data/app-lib ON INSTALL. Path A can then try to
         exec those extracted copies directly; Path B always has the assets copy
         as a final fallback.
         Drawback: adds ~90MB installed footprint vs false.
         We can flip this back to false AFTER verifying Path B fallback alone
         works consistently on every target device (keeping Path B is the key).
    -->
    <application
        android:name=".BadukNextApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.BadukNext"
        android:extractNativeLibs="true"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.BadukNext">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
