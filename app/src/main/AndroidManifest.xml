<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!--
         APK SHRINK v2 (2026-08-02): extractNativeLibs=false.
         • jniLibs/ is EMPTY (all .so moved to assets/).
         • 0 Java System.loadLibrary calls in the codebase — no JNI loading.
         • KataGo child-process loads its deps FROM filesDir only:
             assets/libkatago.so  → filesDir/libkatago.so (exec'd)
             assets/deps/*.so     → filesDir/*.so        (LD_LIBRARY_PATH=filesDir)
         Setting this to false means the PackageManager will NOT waste space
         extracting jniLibs copies to /data/app-lib (there are none anyway).
         Also guarantees 4-byte-aligned uncompressed alignment for any future
         accidental jniLib addition (Android R+ requirement).
    -->
    <application
        android:name=".BadukNextApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.BadukNext"
        android:extractNativeLibs="false"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.BadukNext">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
