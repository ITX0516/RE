<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!--
         AI-START RELIABILITY REVERT (2026-08-02): extractNativeLibs=true.
         • After "Failed to start AI" on 14.7MB build we revert to the fully
           conservative, historically-tested default: PackageManager MUST
           extract every jniLibs/*.so to /data/app-lib at install time.
         • jniLibs now has 6 .so: libkatago.so + 5 ld.so deps.
         • With extraction guaranteed, context.applicationInfo.nativeLibraryDir
           points at a REAL directory with real files on-disk — our KataGo
           child-process can dlopen them via LD_LIBRARY_PATH without ever
           having to copy them itself, and linker namespace restrictions are
           much less likely to break dlopen() for a same-UID exec() child.
         • This is the SAME configuration used by the upstream badukai build
           and every single pre-shrink-release APK that ever started AI OK.
         • APK/installed-size cost: ~6MB jniLibs + extracted 6MB = ~12MB total.
           Acceptable for guaranteed "engine starts first try" behaviour.
    -->
    <application
        android:name=".BadukNextApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.BadukNext"
        android:extractNativeLibs="true"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.BadukNext">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
