@rem Windows batch stub for gradle wrapper.
@rem Same note: run `gradle wrapper --gradle-version 8.5` on your local machine first
@rem to generate gradle-wrapper.jar, gradlew.bat, etc. This file is a placeholder.
@echo off
echo gradlew.bat stub — please run `gradle wrapper --gradle-version 8.5` locally first.
exit /b 1
